"""Auto-generated by scripts/generate_from_openapi.py — do not edit by hand."""

from typing import Any

from fastmcp import Context, FastMCP
from fastmcp.dependencies import Depends
from pydantic import Field

from onetrust_api.auth import get_client


def register_compliance_automation_tools(mcp: FastMCP):
    @mcp.tool(tags={"compliance_automation"})
    async def onetrust_compliance_automation(
        action: str = Field(
            description="Action to perform. One of: 'find_all_initiatives_by_filter_using_post', 'get_initiative_using_get', 'update_initiative_using_put'"
        ),
        params_json: str = Field(
            default="{}",
            description="JSON string of parameters (path, query, and body fields) for the action.",
        ),
        client=Depends(get_client),
        ctx: Context | None = Field(
            default=None, description="MCP context for progress reporting"
        ),
    ) -> Any:
        """Manage OneTrust compliance automation operations."""
        if ctx:
            await ctx.info(f"Executing onetrust_compliance_automation action: {action}")
        import json

        try:
            kwargs = json.loads(params_json) if params_json else {}
        except Exception as e:
            return {"error": f"Invalid params_json: {e}"}
        if not isinstance(kwargs, dict):
            return {"error": "params_json must decode to a JSON object"}
        kwargs = {k: v for k, v in kwargs.items() if v is not None}

        if action == "find_all_initiatives_by_filter_using_post":
            return client.find_all_initiatives_by_filter_using_post(**kwargs)
        elif action == "get_initiative_using_get":
            return client.get_initiative_using_get(**kwargs)
        elif action == "update_initiative_using_put":
            return client.update_initiative_using_put(**kwargs)
        raise ValueError(f"Unknown action: {action}")
