[pytest]
timeout = 120
asyncio_mode = auto
testpaths = tests
markers =
    integration: Integration tests (require live OneTrust credentials)
addopts = -m "not integration"
filterwarnings =
    ignore:.*exclude_args.*
